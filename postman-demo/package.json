{
  "name": "postman-demo",
  "version": "1.0.0",
  "description": "Postman API tests for Restful Booker running in CI",
  "scripts": {
    "test": "newman run tests/restful-booker.postman_collection.json --reporters cli,junit --reporter-junit-export test-results/junit-report.xml",
    "test:verbose": "newman run tests/restful-booker.postman_collection.json --reporters cli,junit --reporter-junit-export test-results/junit-report.xml --verbose"
  },
  "keywords": [
    "postman",
    "newman",
    "api-testing",
    "ci"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "newman": "^6.2.1"
  }
}
